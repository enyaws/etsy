<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :items %><br>
    <%= f.number_field :items %>
  </div>
  <div class="field">
    <%= f.label :tracking_number %><br>
    <%= f.number_field :tracking_number %>
  </div>
  <div class="field">
    <%= f.label :shipped %><br>
    <%= f.datetime_select :shipped %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
